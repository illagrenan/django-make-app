language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
install:
    - pip install -r requirements.txt
script:
    - inv test
    - inv coverage
    - python setup.py build
    - python setup.py sdist
after_success:
    - coveralls
notifications:
    email: false
